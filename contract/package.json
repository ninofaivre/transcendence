{
    "name": "contract",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "rm -rf dist && tsup src/index.ts --format cjs,esm --dts",
        "dev": "pnpm run build -- --watch"
    },
    "devDependencies": {
        "@nestjs/common": "^10.0.5",
        "tsup": "^7.1.0"
    },
    "dependencies": {
        "@anatine/zod-openapi": "^2.0.1",
        "prisma-generated": "workspace:*",
        "zod": "^3.21.4"
    }
}
